# Copy this file to .env and edit values as needed for local development
# Vite: environment variables exposed to the client must be prefixed with VITE_

# Base URL for the backend API (no trailing slash). If empty the frontend will call relative `/api/*`.
VITE_API_URL=http://localhost:8000
